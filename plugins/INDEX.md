# Plugin index

<!-- GENERATED by `npm run gen:index` — do not edit by hand. -->

128 plugins across 17 categories. Path is relative to `plugins/`.

The folder mirrors the ID: plugin `video.trim` lives at `video/trim.ts`.
Sub-systems get a sub-folder: `voice.voicebox_health` → `voice/voicebox/health.ts`.

## Contents
- [analyze](#analyze) — 9
- [audio](#audio) — 16
- [brand](#brand) — 3
- [browser](#browser) — 2
- [distribute](#distribute) — 4
- [edit](#edit) — 3
- [effects](#effects) — 6
- [export](#export) — 6
- [fx](#fx) — 7
- [image](#image) — 18
- [music](#music) — 2
- [qc](#qc) — 3
- [render](#render) — 6
- [subtitle](#subtitle) — 5
- [transitions](#transitions) — 1
- [video](#video) — 25
- [voice](#voice) — 12

## analyze

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `analyze.scene_audit` | Per-scene probe + cross-check + final assembly QC. Returns a JSON audit report. | ts | `analyze/scene_audit.ts` |
| `analyze.video` | QC report: black frames, freeze frames, audio peaks, codec/duration/aspect/fps. | ts | `analyze/video.ts` |
| `audio.onset` | Measures onsets-per-minute from an audio file, classifies the track as calm/mid/energetic, returns the matching target BPM and top-50 onset timestamps. | ts | `audio/onset.ts` |
| `image.aesthetic` | Returns a 0-5 score: +2 resolution >= 1280x720, +1 >= 1920x1080, +1 sane aspect ratio, +1 file size > 200KB, +1 valid bit-depth. No LLM. | ts | `image/aesthetic.ts` |
| `image.dedup` | Compute dHash (9x8 grayscale, 64-bit) for each image, cluster via Hamming distance threshold. Returns groups so the agent can prune duplicates. | ts | `image/dedup.ts` |
| `image.relevance` | Returns Jaccard similarity (0-1) between query keywords and image tag list. Pure set math, no LLM. | ts | `image/relevance.ts` |
| `text.script_parse` | Splits a script into paragraphs + sentences, identifies [Visual:]/[Text:] cue blocks as scenes, and emits per-scene keywords and reading-time. | ts | `text/script_parse.ts` |
| `video.dedup` | Sample frames, dHash each (9x8 grayscale -> 64-bit), cluster near-duplicates via Hamming distance. Returns timestamped groups so the agent can dedup. | ts | `video/dedup.ts` |
| `video.scene_detect` | Deterministic scene-change detection via ffmpeg select=gt(scene,TH). Returns cut times + optional smart re-assembly. | ts | `video/scene_detect.ts` |

## audio

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `audio.beat` | Deterministic beat/BPM/onset detection via ffmpeg astats. Outputs a regularised beat grid for beat-synced cutting. | ts | `audio/beat.ts` |
| `audio.denoise` | Spectral denoise for audio. afftdn with a fixed floor or adaptive profile. | ts | `audio/denoise.ts` |
| `audio.duck` | Sidechain-compress or volume-envelope a music bed so the voiceover stays clear. | ts | `audio/duck.ts` |
| `audio.eq` | Shape audio with parametric EQ bands, plus optional noise gate and compressor. | ts | `audio/eq.ts` |
| `audio.fade` | Add fade-in and/or fade-out to an audio file. | ts | `audio/fade.ts` |
| `audio.info` | Return duration, codec, sample rate and loudness stats for an audio file. | ts | `audio/info.ts` |
| `audio.lufs_for_platform` | Two-pass loudnorm to platform-specific LUFS targets (YouTube -14, Reels -16, Podcast -16, Broadcast -23, Music -14, Headphones -20). Custom also supported. | ts | `audio/lufs_for_platform.ts` |
| `audio.master` | One-shot final audio: denoise + highpass + compressor + EBU R128 loudnorm + optional sidechain ducking. | ts | `audio/master.ts` |
| `audio.merge` | Mix two or more audio files together (e.g. voiceover + background music). | ts | `audio/merge.ts` |
| `audio.mux` | Replace a video's audio track, and optionally keep the original audio mixed underneath. | ts | `audio/mux.ts` |
| `audio.normalize` | Apply EBU R128 loudness normalization (loudnorm) to an audio file. | ts | `audio/normalize.ts` |
| `audio.remove_silence` | Cut silent passages out of an audio file. | ts | `audio/remove_silence.ts` |
| `audio.sfx` | Deterministic SFX from ffmpeg signal sources (blip, click, whoosh, riser, impact, boom, chime, pad, wind, rain, swoosh, glitch, laser, ...). | ts | `audio/sfx.ts` |
| `audio.speed` | Speed up or slow down audio while preserving pitch (atempo). | ts | `audio/speed.ts` |
| `audio.trim` | Cut a segment out of an audio file. | ts | `audio/trim.ts` |
| `audio.volume` | Scale the volume of an audio file (dB gain or multiplier). | ts | `audio/volume.ts` |

## brand

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `brand.kit` | Bundle: logo corner watermark, intro card, outro card, optional cinemascope letterbox bars. | ts | `brand/kit.ts` |
| `text.hook` | Pick one of 7 deterministic opener templates and plug in the script's strongest keywords. Deterministic given seed. | ts | `text/hook.ts` |
| `text.seo` | Deterministic, no-LLM title + description + hashtag bundle derived from the script via stopword-filtered keyword frequency. | ts | `text/seo.ts` |

## browser

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `browser.act` | Run a list of browser actions (navigate, click, fill, type, press, hover, select, scroll, wait, evaluate, screenshot) with Playwright auto-waiting. | ts | `browser/act.ts` |
| `browser.open` | Open a URL in Playwright Chromium, wait for it to settle, and save a screenshot. Handles cookie banners and dark mode. | ts | `browser/open.ts` |

## distribute

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `delivery.archive` | Reads the current published version under <deliveriesRoot>/<projectId>/, moves that version folder into <archiveRoot>/<projectId>/<version>-<timestamp>. | ts | `delivery/archive.ts` |
| `delivery.publish` | Copies a file to <outDir>/<projectId>/<version>/<basename>, writes a MANIFEST.json (sha1, size, mtime), and updates <projectId>/LATEST to point to the current version. | ts | `delivery/publish.ts` |
| `delivery.revision` | Bumps the patch component of the project's current version (v1.0 -> v1.1) and publishes the new file under that version tag. Reads LATEST marker to find the current version. | ts | `delivery/revision.ts` |
| `export.derivative` | One input, many outputs: 9:16, 16:9, 1:1, plus thumbnail PNG. | ts | `export/derivative.ts` |

## edit

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `edit.beat_cut` | Convert an audio.beat grid into an explicit clip list for render.timeline, so every cut lands on an onset (or beat). | ts | `edit/beat_cut.ts` |
| `edit.ops` | Apply a batch of timeline operations (delete/insert/reorder/update/retime) to a JSON timeline spec. Returns the new timeline + a log of applied ops. | ts | `edit/ops.ts` |
| `video.scene_split` | Split a long clip into N sub-clips: evenly (mode=equal, count=N) or at explicit comma-separated timestamp marks (mode=marks). Uses lossless trim+concat. | ts | `video/scene_split.ts` |

## effects

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `effects.color_grade` | Advanced colour: animated film grain, halation bloom, .cube LUT, lift/gamma/gain wheels, bleach bypass. | ts | `effects/color_grade.ts` |
| `effects.look` | Apply a named colour grade (cinematic, vivid, neon, teal-orange, bleach, warm, cool, bw, sepia, vintage) to a video or image. | ts | `effects/look.ts` |
| `effects.style` | Full look recipe (grade + grain + vignette + sharpening): noir, sunset, cyberpunk, golden, arctic, moody, pastelDream, horrorDesat, documentary, hdr, muted, neonNight. | ts | `effects/style.ts` |
| `effects.video` | Apply a named effect to a video. Supported: film-grain, vignette, letterbox, mirror, black-white, color-pop, blur, sharpen, shake, zoom-punch, edge-glow, slow-shutter | ts | `effects/video.ts` |
| `image.remove_bg` | Local rembg (U-2-Net / ISNet) background removal. Writes a PNG with alpha. | py | `image/remove_bg.json` |
| `video.denoise` | Spatial + temporal denoise (hqdn3d). Optional chroma-only mode to keep skin tones clean. | ts | `video/denoise.ts` |

## export

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `browser.extract` | Read a page in headless Chromium and extract its title, meta, headings, text, links and images as JSON. | ts | `browser/extract.ts` |
| `browser.pdf` | Print a URL or local HTML file to PDF using headless Chromium. | ts | `browser/pdf.ts` |
| `export.contact_sheet` | Build a grid of frames from a video (contact sheet) for visual review. | ts | `export/contact_sheet.ts` |
| `export.gif` | Convert a video segment into an animated GIF. | ts | `export/gif.ts` |
| `export.probe` | Report duration, size, codecs, streams, and run a black-frame check. | ts | `export/probe.ts` |
| `export.reframe` | Fit a video into 9:16 / 16:9 / 1:1 / 4:5 by padding or cropping. | ts | `export/reframe.ts` |

## fx

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `effects.genre` | Applies a single coherent genre look via a tuned ffmpeg filter chain. 14 packs: cinematic, anime, retro-80s, lofi, documentary, vintage-film, cyberpunk, vhs, scifi-clean, horror, news-broadcast, music-video, dream-soft, noir, pop-commercial. | ts | `effects/genre.ts` |
| `fx.chroma_key` | Remove a colour from the background of a clip. Outputs WebM with alpha or mp4 over a coloured background. | ts | `fx/chroma_key.ts` |
| `fx.compare` | Pick the same timestamp from N input videos, scale each to 480x270, tile into a 2-column (or N-column) contact sheet, draw each label. Deterministic, no overlay alignment needed. | ts | `fx/compare.ts` |
| `fx.speed_ramp` | Constant speed, accelerate, decelerate, punch-in, or punch-out via setpts+tpad. | ts | `fx/speed_ramp.ts` |
| `fx.stabilize` | Two-pass vidstab stabilisation (detect + transform) for handheld clips. | ts | `fx/stabilize.ts` |
| `fx.transition_effect` | Time-windowed transition effects: glitch, lightLeak, whipPan, flash, rgbSplit, zoomBlur. Split-concat windowing means every filter works, even ones without timeline support. | ts | `fx/transition_effect.ts` |
| `fx.vintage` | Vintage, sepia, bleach, noir, polaroid, 70s, 80s, VHS, dreamy, cold, warm, punchy, pastel, or mono. | ts | `fx/vintage.ts` |

## image

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `browser.mockup` | Render a URL or local HTML inside a browser-window or phone frame on a styled background. | ts | `browser/mockup.ts` |
| `browser.screenshot` | Render a URL or local HTML file in headless Chromium and save it as PNG/JPG. | ts | `browser/screenshot.ts` |
| `browser.scroll_capture` | Scroll a page in even steps, screenshotting the viewport at each stop. Good for Ken Burns pans and long-page QA. | ts | `browser/scroll_capture.ts` |
| `image.canvas` | Draw into a real HTML5 canvas with caller-supplied JavaScript and save the result as an image. | ts | `image/canvas.ts` |
| `image.convert` | Convert an image to another format, optionally setting JPEG quality. | ts | `image/convert.ts` |
| `image.create` | Rasterise SVG or HTML+CSS markup into a PNG/JPG image using a real browser engine. | ts | `image/create.ts` |
| `image.crop` | Crop a rectangular region out of an image. | ts | `image/crop.ts` |
| `image.download` | Search and download stock images from pexels \| pixabay \| openverse \| wikimedia. | ts | `image/download.ts` |
| `image.filter` | Apply a named visual filter. Supported: blur, sharpen, grayscale, sepia, vignette, noise, emboss, edge, pixelate, invert, posterize | ts | `image/filter.ts` |
| `image.flip` | Flip an image horizontally or vertically. | ts | `image/flip.ts` |
| `image.generate` | Create an image via fal.ai or Replicate text-to-image. Requires a key. Does NOT auto-fallback. | ts | `image/generate.ts` |
| `image.grade` | Adjust brightness, contrast, saturation and gamma of an image. | ts | `image/grade.ts` |
| `image.info` | Return dimensions, format and file size for an image. | ts | `image/info.ts` |
| `image.resize` | Resize an image to an explicit width/height (or fit within a box). | ts | `image/resize.ts` |
| `image.rotate` | Rotate an image by an arbitrary angle (or 90° steps). | ts | `image/rotate.ts` |
| `image.text` | Burn text onto an image (title card, caption, lower third). | ts | `image/text.ts` |
| `image.watermark` | Overlay a watermark/logo image onto an image. | ts | `image/watermark.ts` |
| `screen.shot` | Take one still screenshot of the desktop (or one window by title). | ts | `screen/shot.ts` |

## music

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `music.download` | Download CC-licensed music from internet-archive. | ts | `music/download.ts` |
| `music.generate` | Synthesise a simple royalty-free background track (WAV) procedurally. | py | `music/generate.py` |

## qc

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `qc.asset` | Validate one media file: min resolution, aspect ratio, duration, codec, sha256. | ts | `qc/asset.ts` |
| `qc.gate` | Runs deterministic gates against a media file: file exists, has video stream, duration in range, duration under platform cap, file size floor, audio stream present if required, resolution floor. | ts | `qc/gate.ts` |
| `qc.placeholder_check` | Downscale to 64x64 grayscale, read signalstats YSTD. YSTD <= 8 = placeholder (FAIL). 8 < YSTD <= 14 = low-information (WARN). Otherwise PASS. | ts | `qc/placeholder_check.ts` |

## render

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `motion.canvas` | Render an HTML5 Canvas animation to MP4/WebM by sampling one frame at a time. Supports optional audio, alpha (webm), and custom frame functions. | ts | `motion/canvas.ts` |
| `motion.effect` | Ken Burns zoom/pan, handheld shake, parallax drift, punch-in/out. Works on stills (with duration) or clips. | ts | `motion/effect.ts` |
| `motion.remotion` | Bundle and render a caller-authored Remotion (React) composition. The composition code can use the full Remotion API: useFrame, spring, interpolate, transitions, shapes, paths, captions, kinetic text, etc. | ts | `motion/remotion/render.ts` |
| `motion.remotion_template` | Render a prebuilt Remotion composition (lower-third, title-card, end-cta, countdown, progress-bar, kinetic-text, bar-chart, logo-reveal, spectrum, confetti, stat-counter, quote-card, split-screen, typewriter, timeline, list-reveal, waveform, glitch-title, testimonial, product-card) by name. | ts | `motion/remotion/template.ts` |
| `render.slideshow` | Render a sequence of images into a video, with optional Ken Burns motion and audio. | ts | `render/slideshow.ts` |
| `render.timeline` | Assemble an explicit list of clips (+ optional audio and subtitles) into the final video. | ts | `render/timeline.ts` |

## subtitle

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `subtitle.burn` | Permanently render an SRT/ASS subtitle file onto a video. | ts | `subtitle/burn.ts` |
| `subtitle.convert` | Convert SRT↔VTT and optionally shift every cue by an offset. | ts | `subtitle/convert.ts` |
| `subtitle.create` | Build an SRT or VTT subtitle file from timed cues. | ts | `subtitle/create.ts` |
| `subtitle.karaoke` | Even-distributed word captions from a word list, or precise cues from an SRT-shaped array. | ts | `subtitle/karaoke.ts` |
| `subtitle.syllable` | Synthesise per-syllable caption timings (165 ms/syllable, clamped [120,600], 40 ms inter-word gap). Emits .srt and an 8-style CSS palette. | ts | `subtitle/syllable.ts` |

## transitions

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `transitions.xfade` | Join two clips with an ffmpeg xfade transition. | ts | `transitions/xfade.ts` |

## video

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `browser.record` | Record a URL / local HTML file to MP4 or WebM while scrolling, one captured frame at a time. | ts | `browser/record.ts` |
| `browser.record_flow` | Record video of a website while driving it (navigate, scroll, click, type). Produces MP4 via Playwright + ffmpeg. | ts | `browser/record_flow.ts` |
| `screen.record` | Capture the desktop (or one window by title) to MP4 using the platform screen-grabber. | ts | `screen/record.ts` |
| `video.animate` | Animate a still image into a short motion clip using a local ComfyUI server with AnimateDiff. Needs ComfyUI running — for the no-dependency path use video.from_images. | ts | `video/animate.ts` |
| `video.crop` | Crop a video to a rectangle, or to an aspect ratio with auto-centering. | ts | `video/crop.ts` |
| `video.download` | Search and download stock video clips from pexels \| pixabay \| wikimedia. | ts | `video/download.ts` |
| `video.extract_audio` | Pull the audio track out of a video into an audio file. | ts | `video/extract_audio.ts` |
| `video.extract_frames` | Export still images from a video — every N seconds, a fixed count, or at explicit timestamps. | ts | `video/extract_frames.ts` |
| `video.fade` | Add a fade from/to black at the start and/or end of a clip. | ts | `video/fade.ts` |
| `video.from_images` | Convert still images into a video with optional Ken Burns motion and crossfade transitions. Local ffmpeg — no API key. (For AI motion see video.generate.) | ts | `video/from_images.ts` |
| `video.generate` | Create a video via fal.ai text-to-video or image-to-video. Requires FAL_KEY. | ts | `video/generate.ts` |
| `video.grade` | Adjust brightness, contrast, saturation and gamma of a video. | ts | `video/grade.ts` |
| `video.info` | Return duration, dimensions, codecs and streams for a video file. | ts | `video/info.ts` |
| `video.merge` | Join multiple clips end to end. Re-encodes so mixed sources always work. | ts | `video/merge.ts` |
| `video.overlay` | Overlay one video/image on top of another (PiP). | ts | `video/overlay.ts` |
| `video.remove_silence` | Cut silent passages out of a video (also removes the matching picture). | ts | `video/remove_silence.ts` |
| `video.resize` | Scale a video to an explicit size or to a target short-edge height. | ts | `video/resize.ts` |
| `video.reverse` | Play a clip backwards (re-encodes, no audio). | ts | `video/reverse.ts` |
| `video.rotate` | Rotate a video by 90 / 180 / 270 degrees, or an arbitrary angle. | ts | `video/rotate.ts` |
| `video.speed` | Speed up or slow down a video (and optionally its audio). | ts | `video/speed.ts` |
| `video.text` | Burn text onto a video, optionally only during a time window. | ts | `video/text.ts` |
| `video.thumbnail` | Grab a single frame from a video as an image. | ts | `video/thumbnail.ts` |
| `video.transform` | Animate a clip’s scale, position and rotation between keyframes, composited onto a background at a constant opacity. | ts | `video/transform.ts` |
| `video.trim` | Cut a segment out of a video by start time and duration. | ts | `video/trim.ts` |
| `video.watermark` | Overlay a logo/watermark image onto a video. | ts | `video/watermark.ts` |

## voice

| id | what it does | engine | file |
| --- | --- | --- | --- |
| `voice.clone` | Synthesise speech in a cloned voice using Coqui XTTS-v2. Requires 'pip install TTS' and a reference audio. | py | `voice/clone.py` |
| `voice.dialogue` | Synthesise a multi-speaker script into one sequenced audio track, with a different voice per speaker. | ts | `voice/dialogue.ts` |
| `voice.list_voices` | List available Edge-TTS voices, optionally filtered by locale (e.g. en-US). | py | `voice/list_voices.py` |
| `voice.stt` | Transcribe speech from an audio/video file using faster-whisper. | py | `voice/stt.py` |
| `voice.tts` | Synthesise natural speech to an MP3/WAV file using Edge-TTS. | py | `voice/tts.py` |
| `voice.voicebox_clone` | Create a cloned Voicebox voice profile from reference audio + transcript. Returns a profile id you can speak through. | ts | `voice/voicebox/clone.ts` |
| `voice.voicebox_health` | Ping the local Voicebox TTS server (default http://localhost:17493) and report its health. | ts | `voice/voicebox/health.ts` |
| `voice.voicebox_history` | List / search Voicebox generations, read stats, or re-download the audio of a previous generation by id. | ts | `voice/voicebox/history.ts` |
| `voice.voicebox_models` | Inspect Voicebox engine status, preload a model, or unload one to free VRAM before switching engines. | ts | `voice/voicebox/models.ts` |
| `voice.voicebox_profiles` | List, create, inspect or delete Voicebox voice profiles (cloned / preset / designed). | ts | `voice/voicebox/profiles.ts` |
| `voice.voicebox_server` | Lifecycle control for the vendored Voicebox TTS backend (vendor/voicebox/speech). Actions: start, stop, restart, status. | ts | `voice/voicebox/server.ts` |
| `voice.voicebox_speak` | Generate speech through a Voicebox voice profile (cloned / preset / designed) and save the audio file. | ts | `voice/voicebox/speak.ts` |
